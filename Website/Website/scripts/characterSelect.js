document.getElementById("select_btn").addEventListener("click", CharSelected);
function CharSelected() {
    //send message to server that character is selected
    // doSend("CharSelected");
    console.log("CharSelected");
    finalMessage = "1";
}